# Cloudflare Pages SPA fallback: serve index.html for client-side routes
# (/word/:word, /profile/:address, /u/:username, /market, /top, /activity, /watchlist).
# Static assets are served first; everything else falls through to the app.
/*    /index.html   200
